import React from 'react';

function UserPage() {

    //  function getUser() {

    //         fetch(`/users/${viewUser.id}`)
    //             .then(resp => resp.json())
    //             .then(user => {
    //                 setUser(user)
    //             })
       
    // }

    return (
        <div>

        </div>
    );
}
export default UserPage